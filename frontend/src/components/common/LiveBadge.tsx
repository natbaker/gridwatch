export function LiveBadge() {
  return (
    <span className="inline-flex items-center gap-1.5 px-2 py-0.5 bg-accent/20 rounded text-xs font-semibold text-accent">
      <span className="w-2 h-2 bg-accent rounded-full animate-breathe" />
      LIVE
    </span>
  )
}
